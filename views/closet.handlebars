<h1>Closet</h1>
<nav>
	<ul>
		<li><a href="./wardrobe">Wardrobe</a></li>
		<li><a href="./closet">Closet</a></li>
		<li><a href="./favorites">Favorites</a></li>
	</ul>
</nav>
<div class="outfit-pieces">
	<div class="piece">
		<img src="{{headwear.imageUrl}}" alt="{{headwear.description}}" />
		<p>{{headwear.description}}</p>
	</div>
	<div class="piece">
		<img src="{{bodywear.imageUrl}}" alt="{{bodywear.description}}" />
		<p>{{bodywear.description}}</p>
	</div>
	<!-- Add more outfit pieces -->
</div>

<div class="filters">
	<label for="id-filter">Filter by ID:</label>
	<select id="id-filter">
		<option value="all">All</option>
		<option value="headwear">Headwear</option>
		<option value="bodywear">Bodywear</option>
		<!-- Add more filter options -->
	</select>

	<label for="owned-filter">Owned:</label>
	<input type="checkbox" id="owned-filter" />
</div>